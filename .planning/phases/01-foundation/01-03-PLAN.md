---
phase: 01-foundation
plan: 03
type: execute
wave: 3
depends_on: [01-01, 01-02]
files_modified: [services/uploadService.ts, services/supabase.ts, package.json]
autonomous: true
requirements: [FR-4.1, FR-4.2, NFR-1.3, NFR-2.1]
user_setup:
  - service: Supabase
    why: "Cloud storage and background sync"
    dashboard_config:
      - task: "Create 'observations' storage bucket"
        location: "Supabase Dashboard -> Storage"

must_haves:
  truths:
    - "Photos are uploaded to Supabase Storage in the background."
    - "System uses Signed Upload URLs for secure background transfers."
    - "Sync status in store is updated upon upload completion or failure."
  artifacts:
    - path: "services/uploadService.ts"
      provides: "Background upload orchestration"
    - path: "services/supabase.ts"
      provides: "Signed URL generation logic"
  key_links:
    - from: "services/uploadService.ts"
      to: "expo-task-manager"
      via: "TaskManager.defineTask"
---

<objective>
Implement background synchronization for captured photos to Supabase Storage, ensuring high reliability and security via Signed URLs.

Purpose: Enable cloud persistence with background resiliency.
Output: Background upload service and Supabase integration logic.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phase1/RESEARCH.md
@.planning/phases/01-foundation/01-02-SUMMARY.md
@services/supabase.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Background Sync Dependencies</name>
  <files>package.json</files>
  <action>
    Install expo-task-manager and expo-background-fetch using 'npx expo install'. Per RESEARCH.md Standard Stack.
  </action>
  <verify>
    <automated>grep -E "expo-task-manager|expo-background-fetch" package.json</automated>
  </verify>
  <done>Required dependencies for background tasks are installed.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Signed URL Retrieval</name>
  <files>services/supabase.ts</files>
  <action>
    Add a helper function to services/supabase.ts to generate a Signed Upload URL for a given path in the 'observations' bucket. Ensure it handles authentication checks.
  </action>
  <verify>
    <automated>npm test services/supabase.test.ts</automated>
  </verify>
  <done>App can securely request upload permissions from Supabase.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Background Upload Service</name>
  <files>services/uploadService.ts</files>
  <action>
    1. Define BACKGROUND_PHOTO_UPLOAD task using TaskManager.
    2. Implement uploadPhoto function using FileSystem.createUploadTask with SessionType.BACKGROUND.
    3. Register the task and trigger it when an observation is saved (or when new photos are added).
    4. Upon completion, update the imageStore/observationStore status for that photo.
  </action>
  <verify>
    <automated>npm test services/uploadService.test.ts</automated>
    <human-check>Trigger an upload, background the app, and check Supabase Storage dashboard after a minute to see if the file arrived.</human-check>
  </verify>
  <done>Photos are reliably uploaded to the cloud in the background.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App → Supabase Storage | Background upload tasks must use secure authenticated channels |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | Information Disclosure | Storage Bucket | mitigate | Use Signed URLs to prevent unauthorized direct uploads. |
| T-01-05 | Spoofing | Upload Source | mitigate | Supabase RLS and bucket policies should restrict uploads to authenticated users' own folders. |
| T-01-SC | Tampering | npm installs | mitigate | slopcheck + blocking human checkpoint for [ASSUMED] packages |
</threat_model>

<verification>
Ensure photos are successfully uploaded to Supabase Storage and the local status is updated.
</verification>

<success_criteria>
All photos in an observation are uploaded to the cloud without requiring the app to stay in the foreground.
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-03-SUMMARY.md` when done
</output>
