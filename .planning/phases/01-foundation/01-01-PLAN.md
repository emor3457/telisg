---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [store/imageStore.ts, store/observationStore.ts]
autonomous: true
requirements: [FR-1.2, FR-5.1, FR-5.2, FR-5.3, NFR-2.2]

must_haves:
  truths:
    - "Observations store an array of photo assets rather than a single base64 string."
    - "Photo assets track their upload status (pending, uploading, completed, failed)."
    - "Observation data survives app restarts including photo references."
  artifacts:
    - path: "store/imageStore.ts"
      provides: "Queue-based photo management"
    - path: "store/observationStore.ts"
      provides: "Multi-photo observation persistence"
  key_links:
    - from: "store/observationStore.ts"
      to: "store/imageStore.ts"
      via: "image reference syncing"
---

<objective>
Refactor the data layer to support multiple photos per observation and transition from base64 storage to local file URI references with status tracking.

Purpose: Enable scalable multi-photo capture and reliable offline-first persistence.
Output: Updated Zustand stores with multi-photo support.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phase1/RESEARCH.md
@store/imageStore.ts
@store/observationStore.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update imageStore for Queue Management</name>
  <files>store/imageStore.ts</files>
  <behavior>
    - Add PhotoAsset interface: { id: string, uri: string, status: 'pending' | 'uploading' | 'completed' | 'failed', storagePath?: string }
    - Change state to: { capturedImages: PhotoAsset[] }
    - Add actions: addImage(uri), removeImage(id), updateImageStatus(id, status, storagePath), clearImages()
  </behavior>
  <action>
    Refactor imageStore.ts per D-03. Replace single-image state with a 'capturedImages' array. Use local file URIs as the source of truth, avoiding base64 conversion here.
  </action>
  <verify>
    <automated>npm test store/imageStore.test.ts</automated>
  </verify>
  <done>imageStore supports adding and managing multiple photo assets with status tracking.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update observationStore for Multi-photo Persistence</name>
  <files>store/observationStore.ts</files>
  <behavior>
    - Update ObservationItem interface: Replace imageUri/imageBase64 with images: PhotoAsset[]
    - Ensure persistence works with AsyncStorage for the updated array structure.
  </behavior>
  <action>
    Modify ObservationItem and useObservationStore. Update addObservation to accept the new images array. Ensure the PDF generation logic (which needs base64) is deferred or handled via a helper later (per Pitfall 2 in RESEARCH.md).
  </action>
  <verify>
    <automated>npm test store/observationStore.test.ts</automated>
  </verify>
  <done>Observations can be saved with multiple photo references and survive app restart.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client State → Storage | Local storage of photo URIs must be resilient to data corruption |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | AsyncStorage | accept | Only local file paths stored; images are in app-private directory. |
| T-01-02 | Denial of Service | AsyncStorage | mitigate | Limit photos per observation to 5 to prevent storage bloat. |
</threat_model>

<verification>
Verify that adding multiple photos to the store and reloading the app keeps the photo list intact.
</verification>

<success_criteria>
Observation data model supports up to 5 photos with independent upload statuses.
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-01-SUMMARY.md` when done
</output>
