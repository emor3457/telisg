---
phase: 01-foundation
plan: 03
type: execute
wave: 3
depends_on: [01-01, 01-02]
files_modified: [services/syncService.ts, package.json, app.json]
autonomous: true
requirements: [FR 5.2, FR 5.3, NFR 2.1, NFR 4.1]
user_setup:
  - service: Supabase
    why: "Cloud storage for photo assets"
    dashboard_config:
      - task: "Create 'observations' storage bucket"
        location: "Supabase Dashboard -> Storage"

must_haves:
  truths:
    - "System syncs photos to Supabase Storage in the background."
    - "app.json is configured with required background task plugins."
    - "Sync status (Pending, Syncing, Completed, Error) is visible in the UI."
  artifacts:
    - path: "services/syncService.ts"
      provides: "Background sync orchestration"
    - path: "app.json"
      provides: "Expo plugin configuration for background tasks"
  key_links:
    - from: "services/syncService.ts"
      to: "expo-task-manager"
      via: "task definition"
---

<objective>
Implement background synchronization infrastructure for observations and photo assets.

Purpose: Ensure data reliability and cloud persistence without blocking the user.
Output: Background sync service and updated app configuration.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation/01-02-SUMMARY.md
@app.json
@services/supabase.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Background Sync Dependencies and Config</name>
  <files>package.json, app.json</files>
  <action>
    1. Install 'expo-task-manager' and 'expo-background-fetch'.
    2. Update app.json to include these in the 'plugins' array.
    3. Add necessary permissions to app.json (e.g., BACKGROUND_FETCH).
  </action>
  <verify>
    <automated>grep -E "expo-task-manager|expo-background-fetch" package.json && grep -E "expo-task-manager|expo-background-fetch" app.json</automated>
  </verify>
  <done>Background task infrastructure is configured at the project level.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Signed URL Upload Logic</name>
  <files>services/syncService.ts</files>
  <behavior>
    - uploadFileWithSignedUrl(uri, remotePath): Promise<void>
    - Handle Supabase Storage interactions with authenticated sessions.
  </behavior>
  <action>
    Implement the core upload logic. Use Signed URLs (generated via Supabase service) for secure background uploads as recommended in RESEARCH.md. Ensure status updates in imageStore upon success/failure.
  </action>
  <verify>
    <automated>npm test services/syncService.test.ts</automated>
  </verify>
  <done>Secure file upload logic is ready for background execution.</done>
</task>

<task type="auto">
  <name>Task 3: Background Task Registration</name>
  <files>services/syncService.ts</files>
  <action>
    Define and register the 'BACKGROUND_OBSERVATION_SYNC' task using TaskManager. Register a BackgroundFetch task to periodically trigger sync for any 'pending' or 'failed' assets. Implement exponential backoff for retries per NFR 2.1.
  </action>
  <verify>
    <automated>grep "TaskManager.defineTask" services/syncService.ts</automated>
    <human-check>Trigger a sync, background the app, and verify file arrival in Supabase Storage dashboard.</human-check>
  </verify>
  <done>Observations and photos sync reliably in the background.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App → Supabase Storage | Background network requests |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | Information Disclosure | Signed URLs | mitigate | URLs should have short expiration (e.g., 15 mins). |
| T-01-05 | Resource Exhaustion | Sync Retries | mitigate | Exponential backoff to prevent flooding the server on network issues. |
| T-01-SC | Tampering | npm installs | mitigate | slopcheck + blocking human checkpoint for [ASSUMED] packages |
</threat_model>

<verification>
Ensure sync service correctly registers tasks and uploads files to the cloud.
</verification>

<success_criteria>
Pending observations are automatically synced to the cloud, even if the app is in the background.
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-03-SUMMARY.md` when done
</output>
