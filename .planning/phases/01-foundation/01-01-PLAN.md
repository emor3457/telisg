---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [store/imageStore.ts, store/observationStore.ts, services/locationService.ts, package.json]
autonomous: true
requirements: [FR 1.1, FR 4.1, FR 4.2, FR 5.1, NFR 1.3, NFR 2.2]

must_haves:
  truths:
    - "Observations store an array of photo assets rather than a single URI."
    - "GPS coordinates and altitude are captured and stored with each observation."
    - "Timestamp of capture is recorded."
    - "Photo assets use file URIs instead of base64 to ensure fast startup (NFR 1.3)."
  artifacts:
    - path: "store/imageStore.ts"
      provides: "Multi-photo management with status"
    - path: "store/observationStore.ts"
      provides: "Multi-photo and metadata persistence"
    - path: "services/locationService.ts"
      provides: "GPS capture wrapper using expo-location"
  key_links:
    - from: "store/observationStore.ts"
      to: "services/locationService.ts"
      via: "location data capture"
---

<objective>
Refactor the data layer for multi-photo support and implement GPS metadata capture.

Purpose: Provide the foundation for high-fidelity evidence capture with spatial context.
Output: Refactored stores and a new location service.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@store/imageStore.ts
@store/observationStore.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Location Dependencies</name>
  <files>package.json</files>
  <action>
    Install 'expo-location' using 'npx expo install'. This is required for FR 4.1.
  </action>
  <verify>
    <automated>grep "expo-location" package.json</automated>
  </verify>
  <done>expo-location is added to dependencies.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refactor Stores for Multi-photo and Metadata</name>
  <files>store/imageStore.ts, store/observationStore.ts</files>
  <behavior>
    - PhotoAsset: { id: string, uri: string, status: 'pending' | 'uploading' | 'completed' | 'failed' }
    - ObservationItem: { ..., photos: PhotoAsset[], location?: { latitude: number, longitude: number, altitude: number | null }, timestamp: number }
  </behavior>
  <action>
    Update imageStore.ts to manage an array of PhotoAsset objects. Refactor observationStore.ts to use this array and include location/timestamp fields per FR 4.1 and FR 4.2. Shift from base64 storage to local file URIs to satisfy NFR 1.3 (App startup time impact) and prevent storage bloat. Ensure persistence with AsyncStorage handles the new structure.
  </action>
  <verify>
    <automated>npm test store/imageStore.test.ts && npm test store/observationStore.test.ts</automated>
  </verify>
  <done>Stores support multi-photo assets and geographic metadata.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement Location Service</name>
  <files>services/locationService.ts</files>
  <behavior>
    - getCurrentLocation(): Promise<{ latitude, longitude, altitude }>
    - Handle permission requests and errors gracefully.
  </behavior>
  <action>
    Create a wrapper for expo-location. It should request permissions and return the current position. Use accuracy high for safety-critical data capture per FR 4.1.
  </action>
  <verify>
    <automated>npm test services/locationService.test.ts</automated>
  </verify>
  <done>Reliable GPS capture service is available.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Device GPS → App | Input from OS location services |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | GPS Data | mitigate | Store location only within the app's private observation data. |
| T-01-02 | Privacy | Permissions | mitigate | Request location permissions only when needed for capture. |
</threat_model>

<verification>
Run 'npm test' to ensure store logic and location service correctly handle data structures.
</verification>

<success_criteria>
Observation model stores up to 5 photos, GPS coordinates, and capture timestamp.
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-01-SUMMARY.md` when done
</output>
