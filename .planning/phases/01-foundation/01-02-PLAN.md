---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified: [app/camera/index.tsx, components/PhotoGallery.tsx, app/observations/[id].tsx]
autonomous: false
requirements: [FR-1.1, FR-1.2, FR-1.3, NFR-4.1]

must_haves:
  truths:
    - "User can capture multiple photos in a single camera session."
    - "Camera screen displays a thumbnail of the last captured photo."
    - "Observation form displays a gallery of all captured photos."
    - "AI analysis is triggered for the primary photo (first in array)."
  artifacts:
    - path: "components/PhotoGallery.tsx"
      provides: "Visual management of multiple photos"
    - path: "app/camera/index.tsx"
      provides: "Multi-shot photo capture"
  key_links:
    - from: "app/camera/index.tsx"
      to: "store/imageStore.ts"
      via: "addImage calls"
---

<objective>
Enhance the user experience for capturing and reviewing multiple photos within the observation flow.

Purpose: Provide a professional multi-evidence capture interface.
Output: Updated Camera screen and a new PhotoGallery component.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phase1/RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@app/camera/index.tsx
@app/observations/[id].tsx
@store/imageStore.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update CameraScreen for Multi-shot</name>
  <files>app/camera/index.tsx</files>
  <action>
    Modify CameraScreen to stay in camera view after capture. Use imageStore.addImage(photo.uri) for each shot. Limit to 5 photos total. Display a small preview thumbnail of the last shot and a 'Check' icon button to navigate to the observation form once satisfied.
  </action>
  <verify>
    <automated>Check for use of addImage and limit check in camera/index.tsx via grep</automated>
    <human-check>Open camera, take 3 photos, verify they appear in the thumbnail preview, then click 'Check' to proceed.</human-check>
  </verify>
  <done>Users can take up to 5 photos in one session without screen jumping.</done>
</task>

<task type="auto">
  <name>Task 2: Create PhotoGallery Component</name>
  <files>components/PhotoGallery.tsx</files>
  <action>
    Create a horizontal scrollable gallery component that takes an array of URIs. Allow deleting photos via an 'X' overlay. (Reordering can be a deferred idea for now, but deletions are required per success criteria).
  </action>
  <verify>
    <automated>npm test components/PhotoGallery.test.ts</automated>
  </verify>
  <done>PhotoGallery renders thumbnails and supports deletion.</done>
</task>

<task type="auto">
  <name>Task 3: Integrate Gallery into Observation Form</name>
  <files>app/observations/[id].tsx</files>
  <action>
    Update ObservationDetailScreen to use PhotoGallery instead of a single Image. Pass the first photo's base64 to analyzeObservation for AI hazard detection (per Open Questions in RESEARCH.md). Ensure the 'Save' action persists all images in the array.
  </action>
  <verify>
    <human-check>Enter observation form with multiple photos, verify AI analysis works, and save. Check if all photos are listed in the gallery.</human-check>
  </verify>
  <done>Observation form handles multiple photos and correctly triggers AI analysis on the primary one.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Camera → Filesystem | Captured images must be saved with appropriate permissions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | Tampering | Camera Output | accept | Standard OS-level camera API used. |
</threat_model>

<verification>
Ensure multiple photos can be captured and viewed in the observation form.
</verification>

<success_criteria>
User can capture 5 photos and see them all in the gallery within the observation form.
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-02-SUMMARY.md` when done
</output>
