---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified: [app/camera/index.tsx, components/PhotoGallery.tsx, app/observations/[id].tsx]
autonomous: false
requirements: [FR 1.1, FR 1.2, FR 1.3, FR 4.1]

must_haves:
  truths:
    - "User can take up to 5 photos in one camera session."
    - "Camera captures GPS coordinates automatically for each photo taken."
    - "Gallery allows reordering photos via drag-and-drop or simple controls."
    - "Gallery allows deleting photos."
  artifacts:
    - path: "app/camera/index.tsx"
      provides: "Multi-shot camera with location capture"
    - path: "components/PhotoGallery.tsx"
      provides: "Reorderable thumbnail gallery"
  key_links:
    - from: "app/camera/index.tsx"
      to: "services/locationService.ts"
      via: "coordinate capture"
    - from: "components/PhotoGallery.tsx"
      to: "store/imageStore.ts"
      via: "reorderImages action"
---

<objective>
Enhance camera flow with multi-shot and GPS capture, and build a reorderable gallery UI.

Purpose: Improve evidence quality with spatial context and flexible image management.
Output: Updated Camera and Gallery components.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@app/camera/index.tsx
@app/observations/[id].tsx
@services/locationService.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Multi-shot Camera with GPS Capture</name>
  <files>app/camera/index.tsx</files>
  <action>
    Modify CameraScreen to support multi-shot (up to 5). On each capture, call locationService.getCurrentLocation() and store coordinates in the PhotoAsset metadata (or the observation's primary location if preferred, but capturing per-photo is more accurate per FR 4.1). Display a 'done' button to transition to the form.
  </action>
  <verify>
    <automated>grep -E "locationService|getCurrentLocation" app/camera/index.tsx</automated>
    <human-check>Take a photo, verify GPS toast or log shows coordinates captured, take 3 more photos, then click 'done'.</human-check>
  </verify>
  <done>Camera captures multiple photos with automatic GPS tagging.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reorderable Photo Gallery</name>
  <files>components/PhotoGallery.tsx</files>
  <behavior>
    - Render thumbnails for PhotoAsset array.
    - Implement reorderImages(fromIndex, toIndex) function.
    - Add 'X' button to delete photo.
    - Support drag-and-drop (using react-native-draggable-flatlist or simple swipe/button reordering).
  </behavior>
  <action>
    Build the PhotoGallery component. Ensure it supports both removal and reordering per FR 1.2. Use a smooth animation for reordering if possible (Reanimated).
  </action>
  <verify>
    <automated>npm test components/PhotoGallery.test.ts</automated>
  </verify>
  <done>Users can manage photo order and composition for the observation.</done>
</task>

<task type="auto">
  <name>Task 3: Observation Form Integration</name>
  <files>app/observations/[id].tsx</files>
  <action>
    Update the observation form to use the new PhotoGallery. Pass all photos to the save flow. Ensure the first photo in the reordered array is treated as the 'primary' evidence for AI analysis (Phase 3).
  </action>
  <verify>
    <human-check>Open an observation, reorder photos, delete one, and verify the changes persist after saving.</human-check>
  </verify>
  <done>Observation form fully supports multi-photo management.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User → Gallery | Interaction for reordering/deleting |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | Tampering | Gallery state | mitigate | Validate array indices before performing reorder/delete operations. |
</threat_model>

<verification>
Verify GPS data is present in logs during capture and gallery allows reordering/deletion.
</verification>

<success_criteria>
Photos can be reordered and deleted in the gallery, and GPS data is captured per session/photo.
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-02-SUMMARY.md` when done
</output>
